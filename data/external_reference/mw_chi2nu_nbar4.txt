# Milky Way HMG reduced chi2 (nbar4 = MI+LW+B2+MM baryonic reconstruction).
# N = 196 constraints (152 radial rotation + 44 Wang+2026 vertical), k = 1 free parameter,
# dof = N - k = 195.  MC100 ensemble median (p50).
# Source: Monjo (2026), ../vertical_gravity_hmg,
#   release_final/outputs/nbar4/mc100_chi2_all_models.csv  (HMG k=1 row).
# The Milky Way fit combines radial rotation AND vertical constraints, which lie outside the
# pure rotation-RAR framework used for every other system in this table.  Its n_sigma is
# therefore taken as sqrt(chi2nu) from this published reduced chi2, rather than recomputed from
# the radial RAR alone (that radial-only recompute, dropping the well-fit vertical constraints,
# gives ~2.05 -- an incomplete proxy for the actual MW fit quality).
# chi2nu
3.01
